{
  "name": "@krain/typescript-config",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "rmdir /s /q .turbo || exit 0 && rmdir /s /q node_modules || exit 0 && rmdir /s /q dist || exit 0",
    "build": "if not exist dist mkdir dist && copy *.json dist\\"
  },
  "files": [
    "base.json",
    "nextjs.json",
    "react-library.json",
    "dist"
  ],
  "exports": {
    "./base.json": "./base.json",
    "./nextjs.json": "./nextjs.json",
    "./react-library.json": "./react-library.json"
  },
  "publishConfig": {
    "access": "public"
  }
}
