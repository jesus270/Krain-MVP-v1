## Database Configuration (Postgres)
# The main database URL with connection pooling (recommended for most uses)
DATABASE_URL=postgres://user:password@host/database?sslmode=require

# Database URL without connection pooling (for specific use cases that don't work with pgbouncer)
DATABASE_URL_UNPOOLED=postgresql://user:password@host/database?sslmode=require

## Redis Configuration
# Local development Redis URL
REDIS_URL=redis://localhost:6379 

## Upstash Redis Configuration (for production)
# The main Redis connection URL
KV_URL="rediss://default:xxxxx@xxxxx.upstash.io:6379"
# REST API URL for Upstash Redis
KV_REST_API_URL="https://xxxxx.upstash.io"
# Main API token for full access
KV_REST_API_TOKEN="xxxxx"
# Read-only API token for restricted access
KV_REST_API_READ_ONLY_TOKEN="xxxxx"

## Authentication & Session
# Required in production: Generate with `openssl rand -base64 32`
# Used for encrypting session cookies
SESSION_SECRET=your-secure-session-secret-at-least-32-chars-long

## Application Configuration
# Development:
#   NEXT_PUBLIC_APP_URL=http://localhost:3000
#   NEXT_PUBLIC_APP_DOMAIN=localhost
#   NODE_ENV=development
# Production:
#   NEXT_PUBLIC_APP_URL=https://yourdomain.com
#   NEXT_PUBLIC_APP_DOMAIN=yourdomain.com
#   NODE_ENV=production
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_DOMAIN=localhost
NODE_ENV=development

## Domain Configuration (for cookie settings)
# Development: leave empty or set to localhost
# Production: set to your root domain (e.g., example.com) for cookie sharing across subdomains
DOMAIN=

## Monitoring
# Comma-separated list of allowed origins for monitoring
# Development: MONITORING_ORIGINS=http://localhost:3000
# Production: MONITORING_ORIGINS=https://yourdomain.com
MONITORING_ORIGINS=http://localhost:3000

## Privy Authentication
# Your Privy application ID from the Privy dashboard
NEXT_PUBLIC_PRIVY_APP_ID=your-privy-app-id 